import type { Metadata, Viewport } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'flaunts.me - Your Link in Bio Page',
  description: 'Free link-in-bio page. Add all your social links in one place. Get flaunts.me/@yourname today.',
  keywords: 'link in bio, social media links, bio page, creator tools, C5-DEV',
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  viewportFit: 'cover',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}